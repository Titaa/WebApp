<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
  <%= link_to 'Json', users_path + '.json', class: "btn btn-theme", target: "_blank" %>
</ul>

<%= will_paginate %>
